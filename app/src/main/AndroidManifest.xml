<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prakriti"
        tools:targetApi="31">

        <!-- ✅ SplashScreen is now the launcher activity -->
        <activity
            android:name=".SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" android:exported="true" />
        <activity android:name=".Instruction5" android:exported="false" />
        <activity android:name=".Instruction4" android:exported="false" />
        <activity android:name=".Instruction3" android:exported="false" />
        <activity android:name=".Instruction2" android:exported="false" />
        <activity android:name=".Instruction1" android:exported="false" />
        <activity android:name=".OfflineInstruction" android:exported="false" />

    </application>

</manifest>
